import { appShell,pageHeader,button } from '../core/components.js'; import { icon } from '../core/icons.js'; import { loadState } from '../core/store.js';
export function renderAssets(){const a=loadState().assets;const content=`<div class="page-content">${pageHeader({eyebrow:'Your creative library',title:'Assets',description:'Images, textures, icons, and decorations you can reuse anywhere.',actions:button('Upload assets',{iconName:'upload',attrs:'data-upload-open'})})}<div class="asset-toolbar"><div class="filter-row"><button class="is-active">All assets</button><button>Images</button><button>Icons</button><button>Backgrounds</button><button>Textures</button></div><label class="view-search">${icon('search')}<input placeholder="Search your library"></label></div><section class="upload-drop" data-drop-zone>${icon('upload')}<h2>Drop something beautiful here</h2><p>PNG, JPG, or WebP, up to 10 MB. Files are stored privately in your Supabase asset library.</p><button class="button soft" data-upload-open>Choose files</button><input type="file" accept="image/png,image/jpeg,image/webp" multiple hidden data-file-input></section><section class="section-block"><div class="section-title"><div><h2>Recently used</h2><p>Ready to place on any canvas.</p></div></div><div class="asset-grid" data-asset-grid>${a.map(x=>`<article class="asset-card"><div class="asset-thumb"><img src="${x.src}" alt=""></div><div><strong>${x.name}</strong><small>${x.collection} • ${x.type}</small></div><button class="icon-button" aria-label="Asset options">•••</button></article>`).join('')}</div></section></div>`;return appShell(content,'assets')}
