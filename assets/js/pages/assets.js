import{appShell,pageHeader,button}from'../core/components.js';import{icon}from'../core/icons.js';import{loadState}from'../core/store.js';
export function renderAssets(){const a=loadState().assets;return appShell(`<div class="page-content">${pageHeader({eyebrow:'Your creative library',title:'Assets',description:'Images, textures, icons, and decorations you can reuse on every canvas.',actions:button('Upload assets',{iconName:'upload',attrs:'data-upload-open'})})}<section class="upload-drop" data-drop-zone>${icon('upload')}<h2>Drop something beautiful here</h2><p>PNG, JPG, or WebP, up to 10 MB. Files remain private in Supabase Storage.</p><button class="button soft" data-upload-open>Choose files</button><input type="file" accept="image/png,image/jpeg,image/webp" multiple hidden data-file-input></section><section class="section-block"><div class="section-title"><div><h2>Your library</h2><p>${a.length} reusable assets</p></div></div><div class="asset-grid" data-asset-grid>${a.map(x=>`<article class="asset-card"><div class="asset-thumb"><img src="${x.src}" alt="${x.name}"></div><div><strong>${x.name}</strong><small>${x.collection} • ${x.type}</small></div><button class="icon-button danger-icon" data-delete-asset="${x.id}" aria-label="Delete asset">×</button></article>`).join('')||'<div class="empty-state"><h3>No assets yet</h3><p>Upload photos, textures, and decorations for your visual workspace.</p></div>'}</div></section></div>`,'assets')}
