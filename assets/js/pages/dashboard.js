import {appShell,pageHeader,button,projectCard} from '../core/components.js';import{icon}from'../core/icons.js';import{loadState}from'../core/store.js';
export function renderDashboard(){const s=loadState(),projects=s.projects,upcoming=s.records.filter(r=>r.start).sort((a,b)=>new Date(a.start)-new Date(b.start)).slice(0,5);const content=`<div class="page-content">${pageHeader({eyebrow:new Date().toLocaleDateString('en-US',{weekday:'long',month:'long',day:'numeric'}),title:`Welcome back, ${s.currentUser.name}.`,description:'Plan what is next, capture what happened, and shape each view around the way you think.',actions:button('New project',{iconName:'plus',attrs:'data-open-project-modal'})})}
<section class="dashboard-hero"><div><p class="eyebrow">Your workspace at a glance</p><h2>${projects.length?`${projects.filter(p=>p.state!=='completed'&&p.state!=='memory_archive').length} journeys are still unfolding.`:'Your next adventure begins here.'}</h2><p>Shared information stays dependable while every collaborator gets a personal visual workspace.</p><div class="hero-task-row"><span>${icon('suitcase')} ${projects.length} projects</span><span>${icon('note')} ${s.records.length} records</span><span>${icon('layout')} ${s.views.length} views</span></div></div><button class="button light" data-open-project-modal>Plan a journey ${icon('chevron')}</button></section>
<section class="section-block"><div class="section-title"><div><h2>Your journeys</h2><p>Planning, active trips, and memory archives.</p></div></div><div class="project-grid">${projects.map(projectCard).join('')||'<div class="empty-state"><h3>No projects yet</h3><p>Create your first travel workspace.</p><button class="button primary" data-open-project-modal>New project</button></div>'}</div></section>
<section class="dashboard-columns"><div class="panel"><div class="section-title"><div><h2>Coming up</h2><p>Across every shared project</p></div>${icon('calendar')}</div><div class="event-list">${upcoming.map(r=>`<article><time><strong>${new Date(r.start).getDate()}</strong><span>${new Date(r.start).toLocaleDateString('en-US',{month:'short'}).toUpperCase()}</span></time><div><strong>${r.title}</strong><span>${s.projects.find(p=>p.id===r.projectId)?.title||''} • ${r.subtitle||r.type}</span></div><span class="dot blue"></span></article>`).join('')||'<p class="muted">Nothing scheduled yet.</p>'}</div></div><div class="panel"><div class="section-title"><div><h2>Quick create</h2><p>Start with the shape you need</p></div>${icon('plus')}</div><div class="quick-grid"><button data-open-project-modal>${icon('suitcase')}<span><strong>Trip</strong><small>Plan an adventure</small></span></button><button onclick="location.href='editor.html'">${icon('layout')}<span><strong>Dashboard</strong><small>Design a personal view</small></span></button><button data-open-record-modal>${icon('note')}<span><strong>Record</strong><small>Add shared information</small></span></button><button onclick="location.href='assets.html'">${icon('image')}<span><strong>Memory</strong><small>Upload a photo</small></span></button></div></div></section></div>`;return appShell(content,'dashboard')}
