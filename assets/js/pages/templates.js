import{appShell,pageHeader,button}from'../core/components.js';import{loadState}from'../core/store.js';
const preview=t=>`<div class="template-preview style-${t.style}"><div class="tpl-header"></div><div class="tpl-photo"></div><div class="tpl-lines"><i></i><i></i><i></i></div><div class="tpl-card"></div></div>`;
export function renderTemplates(){const t=loadState().templates,card=x=>`<article class="template-card" data-template-id="${x.id}">${preview(x)}<div><small>${x.category}</small><h3>${x.name}</h3><p>${x.personal?'Created by you':'Workspace template'} • ${x.shared?'Shared':'Private'}</p><div><button class="button soft" onclick="location.href='editor.html?template=${x.id}'">Preview</button><button class="button primary" onclick="location.href='editor.html?template=${x.id}'">Use template</button>${x.personal?`<button class="icon-button danger-icon" data-delete-template="${x.id}">×</button>`:''}</div></div></article>`;return appShell(`<div class="page-content">${pageHeader({eyebrow:'Design library',title:'Templates',description:'Appearance recipes that stay separate from your information.',actions:button('Create template',{iconName:'plus',attrs:'onclick="location.href=\'editor.html?newTemplate=1\'"'})})}<section class="section-block"><div class="section-title"><div><h2>Your templates</h2><p>Private and shared visual systems.</p></div></div><div class="template-grid">${t.map(card).join('')||'<div class="empty-state"><h3>No templates yet</h3><p>Save a canvas design as a reusable template.</p></div>'}</div></section></div>`,'templates')}
