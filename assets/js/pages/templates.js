import { appShell,pageHeader,button } from '../core/components.js'; import { icon } from '../core/icons.js'; import { loadState } from '../core/store.js';
function preview(t){return `<div class="template-preview style-${t.style}"><div class="tpl-header"></div><div class="tpl-photo"></div><div class="tpl-lines"><i></i><i></i><i></i></div><div class="tpl-card"></div></div>`}
export function renderTemplates(){const t=loadState().templates;const card=x=>`<article class="template-card">${preview(x)}<div><small>${x.category}</small><h3>${x.name}</h3><p>${x.personal?'Created by you':'Starter template'} • ${x.shared?'Reusable':'Private'}</p><div><button class="button soft">Preview</button><button class="button primary" onclick="location.href='editor.html'">Use template</button></div></div></article>`;const content=`<div class="page-content">${pageHeader({eyebrow:'Design library',title:'Templates',description:'Appearance recipes that stay separate from your information.',actions:button('Create template',{iconName:'plus',attrs:'onclick="location.href=\'editor.html\'"'})})}<div class="filter-row"><button class="is-active">All</button><button>Personal</button><button>Shared</button><button>Starter</button><span></span><label>${icon('search')}<input placeholder="Search templates"></label></div><section class="section-block"><div class="section-title"><div><h2>Your templates</h2><p>Private visual systems built for the way you think.</p></div></div><div class="template-grid">${t.filter(x=>x.personal).map(card).join('')}</div></section><section class="section-block"><div class="section-title"><div><h2>Starter collection</h2><p>Gentle foundations for travel, projects, and personal dashboards.</p></div></div><div class="template-grid">${t.filter(x=>!x.personal).map(card).join('')}</div></section></div>`;return appShell(content,'templates')}
