import{appShell,pageHeader}from'../core/components.js';import{loadState}from'../core/store.js';
export function renderSettings(){const s=loadState(),w=s.workspaces[0],u=s.currentUser,members=s.members.filter(m=>m.workspaceId===w?.id);return appShell(`<div class="page-content">${pageHeader({eyebrow:'Workspace preferences',title:'Settings',description:'Manage your identity, workspace, collaborators, and account.'})}<div class="settings-grid"><section class="panel"><h2>Your profile</h2><form class="form-grid" data-profile-form><label class="full">Display name<input name="name" value="${u.name||''}" required></label><label class="full">Email<input value="${u.email||''}" disabled></label><div class="form-actions full"><button class="button primary">Save profile</button></div></form></section><section class="panel"><h2>Workspace</h2><form class="form-grid" data-workspace-form><label class="full">Workspace name<input name="name" value="${w?.name||''}" required></label><div class="form-actions full"><button class="button primary">Save workspace</button></div></form></section><section class="panel full-span"><div class="section-title"><div><h2>Collaborators</h2><p>Owners manage access, editors change shared information, viewers can explore.</p></div><button class="button soft" data-open-invite>Invite someone</button></div><div class="member-list">${members.map(m=>{const p=s.profiles.find(x=>x.id===m.userId);return`<article><span class="person-avatar blue">${(p?.name||'?')[0]}</span><div><strong>${p?.name||m.userId}</strong><small>${m.role}</small></div></article>`}).join('')||'<p class="muted">Only you are here right now.</p>'}</div></section><section class="panel full-span danger-zone"><h2>Data and privacy</h2><p>Your canvas layouts are personal unless you explicitly make a view shared. Shared records remain available to workspace collaborators.</p><p>Account deletion and full workspace export are intentionally reserved for the next security-focused release.</p></section></div></div>`,'settings')}
