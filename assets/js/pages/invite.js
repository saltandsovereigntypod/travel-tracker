import{acceptInvite,loadState}from'../core/store.js';
export function renderInvite(){const signed=loadState().auth.signedIn;setTimeout(async()=>{if(!signed)return;const token=new URLSearchParams(location.search).get('token');const status=document.querySelector('[data-invite-status]');try{await acceptInvite(token);status.innerHTML='<h2>You joined the workspace.</h2><p>Your shared projects are ready.</p><a class="button primary" href="dashboard.html">Open Wayfarer</a>'}catch(e){status.innerHTML=`<h2>Invitation could not be accepted</h2><p>${e.message}</p>`}},0);return`<main class="invite-page"><a class="brand" href="index.html"><span><strong>Wayfarer</strong><small>visual workspace</small></span></a><section class="auth-card is-standalone" data-invite-status><p class="eyebrow">Workspace invitation</p><h1>${signed?'Joining your workspace…':'Sign in to accept'}</h1><p>${signed?'We are checking the invitation and adding your account.':'Use the email address that received the invitation, then reopen this link.'}</p>${signed?'':'<button class="button primary" data-open-auth>Sign in or create account</button>'}</section></main>`}
