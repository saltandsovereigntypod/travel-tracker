import { appShell,pageHeader,button } from '../core/components.js'; import { icon } from '../core/icons.js'; import { loadState } from '../core/store.js';
const tabs=['Overview','Timeline','Reservations','Budget','Packing','Places','Notes','Photos','Memory Archive'];
export function renderTrip(){const s=loadState(),p=s.projects[0],records=s.records.filter(r=>r.projectId===p.id);const content=`<div class="page-content trip-page">${pageHeader({eyebrow:'Johnson Family Adventures',title:p.title,description:p.subtitle,actions:`${button('Share',{kind:'soft',iconName:'users'})}${button('Design view',{iconName:'layout',attrs:'onclick="location.href=\'editor.html\'"'})}`})}
<div class="trip-status-line"><span class="project-state planning">Planning</span><span>${icon('calendar')} April 8 to April 22, 2027</span><span>${icon('users')} 2 collaborators</span><span>${icon('clock')} Updated today</span></div>
<nav class="tab-strip">${tabs.map((t,i)=>`<button class="${i===0?'is-active':''}" data-trip-tab="${t}">${t}</button>`).join('')}</nav>
<section class="trip-overview-grid"><div class="trip-main-column"><article class="trip-cover-card"><div class="trip-map-art"><div class="route-line"></div><span class="map-pin pin-one"></span><span class="map-pin pin-two"></span><span class="map-label tokyo">Tokyo</span><span class="map-label kyoto">Kyoto</span></div><div><p class="eyebrow">The intention</p><h2>Move slowly. Notice everything.</h2><p>Two weeks shaped around neighborhoods, trains, good food, quiet mornings, and enough open space for the trip to surprise us.</p><div class="tag-row"><span>Food</span><span>Art</span><span>Shrines</span><span>Slow travel</span></div></div></article>
<div class="panel"><div class="section-title"><div><h2>Next on the timeline</h2><p>The first four shared records</p></div><a href="#">See full timeline</a></div><div class="timeline-list">${records.filter(r=>r.start).slice(0,4).map((r,i)=>`<article><span class="timeline-icon">${icon(r.type==='flight'?'plane':r.type==='hotel'?'home':r.type==='restaurant'?'note':'map')}</span><div><small>${new Date(r.start).toLocaleDateString('en-US',{month:'short',day:'numeric'})}${r.start.includes('T')?' • '+new Date(r.start).toLocaleTimeString('en-US',{hour:'numeric',minute:'2-digit'}):''}</small><strong>${r.title}</strong><span>${r.subtitle||r.type}</span></div><span class="record-status ${r.status}">${r.status.replace('-',' ')}</span></article>`).join('')}</div></div></div>
<aside class="trip-side-column"><div class="panel stat-panel"><h3>Trip readiness</h3><div class="readiness-ring" style="--progress:${p.progress*3.6}deg"><span><strong>${p.progress}%</strong><small>ready</small></span></div><div class="readiness-list"><span><i style="width:100%"></i><b>Flights</b><em>Complete</em></span><span><i style="width:75%"></i><b>Stays</b><em>3 of 4</em></span><span><i style="width:45%"></i><b>Reservations</b><em>5 of 11</em></span><span><i style="width:25%"></i><b>Packing</b><em>Started</em></span></div></div>
<div class="panel budget-card"><div class="section-title"><div><h3>Budget</h3><p>Shared estimate</p></div>${icon('wallet')}</div><strong>$${p.spent.toLocaleString()} <small>of $${p.budget.toLocaleString()}</small></strong><div class="progress-track"><span style="width:${p.spent/p.budget*100}%"></span></div><div><span>Remaining</span><b>$${(p.budget-p.spent).toLocaleString()}</b></div></div>
<div class="panel collaborators-card"><div class="section-title"><div><h3>People and views</h3><p>Same trip, personal layouts</p></div>${icon('users')}</div><article><span class="person-avatar peach">A</span><div><strong>Ash</strong><small>Owner • Journal view</small></div></article><article><span class="person-avatar blue">D</span><div><strong>Daniel</strong><small>Editor • Logistics view</small></div></article><button class="button soft full">${icon('plus')} Invite someone</button></div></aside></section></div>`;return appShell(content,'trip')}
