{
  "name": "wayfarer-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "A visual life workspace engine with travel planning as its first application.",
  "scripts": {
    "start": "npx serve .",
    "test": "node --test tests/*.test.js"
  },
  "engines": { "node": ">=18" }
}
